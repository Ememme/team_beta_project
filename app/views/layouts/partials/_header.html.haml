!!!
%html{:lang => "en"}
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %meta{:content => "width=device-width, initial-scale=1, maximum-scale=1.0", :name => "viewport"}/
    %title Dormitory Manager - bla bla bla!
    / CSS
    = csrf_meta_tags
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload'
    = javascript_include_tag 'application', 'data-turbolinks-track': 'reload'
    %link{:href => "https://fonts.googleapis.com/icon?family=Material+Icons", :rel => "stylesheet"}/
  %body.bg-2
    %nav.bg-1{:role => "navigation"}
      .nav-wrapper.container
        %a#logo-container.brand-logo{:href => root_path} Student Inn

        %ul.right.hide-on-med-and-down
          - if !student_signed_in?

            %li
              = link_to t('profile.login'), new_student_session_path, style: "font-weight: bold"
            %li
              = link_to t('profile.signup'), new_student_registration_path, style: "font-weight: bold"
          - else
            %li
              = link_to "#{t('hello')}, #{current_student.first_name}!", root_path
            %li
              = link_to t('profile.logout'), destroy_student_session_path, :method => :delete

          %li
            - if params[:locale] == "en"
              = link_to 'PL', url_for(locale: :pl)
            - else
              = link_to 'EN', url_for(locale: :en)

        %ul#nav-mobile.side-nav
          %li
            %a{:href => "#"} Navbar Link
        %a.button-collapse{"data-activates" => "nav-mobile", :href => "#"}
          %i.material-icons menu
